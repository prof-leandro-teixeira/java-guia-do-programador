public class ER02 {
    //Public pode ser utilizado livremente pelas instâncias da classe
    //Protected só pode ser utilizado na implementação de subclasses e instâncias dentro do mesmo pacote
    //Private não pode ser utilizado fora da implementação da própria classe
}
